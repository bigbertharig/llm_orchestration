# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
env/
ENV/
ml-env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Config files (machine-specific)
config.json
shared/agents/config.json

# =============================================================================
# Shared folder - selective sync
# =============================================================================
# Default: exclude everything in shared/
shared/*

# SYNC: Agent code (the core engine, minus permissions)
!shared/agents/
shared/agents/__pycache__/
shared/agents/*.pyc
shared/agents/permissions/

# SYNC: Logs (backup value)
!shared/logs/

# NO SYNC: Plans (each plan is its own repo, public or private)
# shared/plans/          - plan folders live here but have their own git repos
# PLAN_FORMAT.md lives in shared/workspace/ (synced with main repo)

# SYNC: Workspace (docs, ideas, human escalations)
!shared/workspace/

# NO SYNC: Runtime state
# shared/brain/         - runtime state, private tasks
# shared/core/          - root-owned, protected, only changed at terminal
# shared/gpus/          - runtime heartbeats
# shared/signals/       - ephemeral control signals
# shared/tasks/         - runtime queue (results go in plan batch history)
# shared/lost+found/    - ext4 system directory

# Temporary and lock files
*.lock
/tmp/

# OS
.DS_Store
Thumbs.db
nul
