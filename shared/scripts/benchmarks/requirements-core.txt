# Core benchmark dependencies (safe default install on rig)
# Install:
#   pip install -r /mnt/shared/scripts/benchmarks/requirements-core.txt

# Main harness + dataset access
lm-eval>=0.4.0
openai>=1.0.0
tenacity>=8.2.0
datasets>=2.14.0
huggingface_hub>=0.19.0

# Code eval baseline
evalplus>=0.2.0

# Utilities
tqdm
requests
pandas
numpy
